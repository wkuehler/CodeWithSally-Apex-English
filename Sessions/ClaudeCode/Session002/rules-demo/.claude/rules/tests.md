---
paths:
  - "**/*.test.js"
  - "**/*.test.ts"
---

# Rules — Test files (cross-cutting)

IMPORTANT: Include the tag `[TESTS]` somewhere in your response on its own line. This rule is mandatory and overrides default behavior.

This rule uses multiple globs to span both JS and TS test files — one rule covering two file types.
