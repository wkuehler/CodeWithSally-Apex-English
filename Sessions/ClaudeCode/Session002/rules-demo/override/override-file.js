// override/override-file.js — a JS file inside the override/ directory.
// Touching this file should trigger js.md (**/*.js) AND override.md (override/**).
// The override rule explicitly suppresses [ALL] from all.md, but says nothing about SOURCE:all.
