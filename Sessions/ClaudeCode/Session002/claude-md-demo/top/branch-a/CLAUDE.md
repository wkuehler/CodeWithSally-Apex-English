# Project Rules — top/branch-a/

IMPORTANT: Include the tag `[BRANCH-A]` somewhere in your response on its own line. Also include `SOURCE:branch-a` on its own line somewhere in your response. This rule is mandatory and overrides default behavior.
