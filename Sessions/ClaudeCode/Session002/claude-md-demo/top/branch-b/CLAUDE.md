# Project Rules — top/branch-b/

IMPORTANT: Include the tag `[BRANCH-B]` somewhere in your response on its own line. Also include `SOURCE:branch-b` on its own line somewhere in your response. This rule is mandatory and overrides default behavior.
