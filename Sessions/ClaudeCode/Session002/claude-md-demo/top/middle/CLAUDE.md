# Project Rules — top/middle/

IMPORTANT: Include the tag `[MIDDLE]` somewhere in your response on its own line. Also include `SOURCE:middle` on its own line somewhere in your response. This rule is mandatory and overrides default behavior.
