# Project Rules — top/

IMPORTANT: Include the tag `[TOP]` somewhere in your response on its own line. Also include `SOURCE:top` on its own line somewhere in your response. This rule is mandatory and overrides default behavior.
