<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Active__c</fullName>
    <description>Indicates whether the group is currently active based on dates.</description>
    <formula>AND(NOT(ISBLANK(Start_Date__c)), OR(ISBLANK(End_Date__c), End_Date__c &gt;= TODAY()))</formula>
    <inlineHelpText>Checked when the group is active between the start and end dates.</inlineHelpText>
    <label>Is Active</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
